/*
Using Javascript, because the .ts file extension for Typescript got the werver confused about MIME types.
 */

function testRectangles(root){
    const R1 = document.createElement("div");
    R1.setAttribute("position","absolute");
    root.appendChild(R1);
}